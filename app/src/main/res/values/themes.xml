<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SpamDetector" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="actionOverflowButtonStyle">@style/MyOverFlowButtonStyle</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SpamDetector" parent="Base.Theme.SpamDetector" />

    <style name="NoUnderlineEditTextStyle">
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="MyOverFlowButtonStyle">
        <item name="android:src">@drawable/filter_menu</item>
        <item name="android:paddingEnd">16dp</item>
    </style>

</resources>